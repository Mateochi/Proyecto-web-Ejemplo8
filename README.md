# Proyecto: Hoja de Vida - Mateo Chica

Este es un proyecto web sencillo y profesional que muestra la Hoja de Vida de **Mateo Chica**. El diseño ha sido mejorado con técnicas modernas de CSS como Glassmorphism, animaciones suaves y fondos gradientes tipo mesh.

## 🚀 Características
- Diseño responsivo y moderno.
- Estructura semántica en HTML5.
- CSS3 puro con variables y animaciones personalizadas.
- Historial de Git completo con simulacros de resolución de conflictos.

## 🛠️ Cómo ejecutar este proyecto localmente

Sigue estos pasos para obtener una copia local y ejecutarla en tu ordenador:

### 1. Clonar el repositorio
Abre tu terminal y ejecuta el siguiente comando:
```bash
git clone https://github.com/Mateochi/Proyecto-web-Ejemplo8.git
```

### 2. Acceder al directorio
Entra en la carpeta del proyecto recién clonada:
```bash
cd Proyecto-web-Ejemplo8
```

### 3. Abrir el proyecto
No necesitas instalar ninguna dependencia, ya que es un sitio estático de HTML y CSS. Simplemente abre el archivo `index.html` en tu navegador preferido:

- **En Windows:**
  ```bash
  start index.html
  ```
- **En macOS:**
  ```bash
  open index.html
  ```
- **En Linux:**
  ```bash
  xdg-open index.html
  ```

O bien, puedes arrastrar directamente el archivo `index.html` a una pestaña abierta de tu navegador.

## 🌿 Estructura de Ramas

Este repositorio utiliza una estrategia de ramificación para organizar el desarrollo de nuevas funcionalidades:

- **`main`**: Rama principal que contiene la versión estable y final del proyecto. Integra tanto la estructura base como todas las mejoras visuales tras la resolución de conflictos.
- **`feature-mejora-estilo`**: Rama dedicada exclusivamente a la actualización estética. Aquí se implementaron:
  - Fondo de gradiente tipo *mesh*.
  - Efectos de *glassmorphism* (transparencias).
  - Animaciones de entrada de elementos.
  - Refinamiento de fuentes y espaciados.

## 📄 Notas adicionales
Este proyecto fue creado como un ejemplo de flujo de trabajo profesional en Git, incluyendo el uso de ramas (`feature-mejora-estilo`), commits frecuentes y resolución manual de conflictos de fusión.
